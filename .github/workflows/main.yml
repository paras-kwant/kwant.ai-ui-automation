name: Run Cypress UI Tests

on:
  schedule:
    # Nepali 10:02 AM → UTC 04:17 AM
    - cron: "17 4 * * *"
  workflow_dispatch:

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      EMAIL: ${{ secrets.EMAIL }}
      PASSWORD: ${{ secrets.PASSWORD }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      SURGE_TOKEN: ${{ secrets.SURGE_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run Cypress tests and capture results
        id: cypress
        continue-on-error: true
        run: |
          npx cypress run --env email=$EMAIL,password=$PASSWORD --reporter spec | tee cypress_output.txt

          # Did tests run?
          if grep -q "All specs passed!" cypress_output.txt || grep -q "failing" cypress_output.txt; then
            echo "tests_ran=true" >> $GITHUB_OUTPUT
          else
            echo "tests_ran=false" >> $GITHUB_OUTPUT
          fi

          # Extract summary line
          SUMMARY_LINE=$(grep "All specs passed!" cypress_output.txt || grep "failing" cypress_output.txt | tail -1)

          TOTAL=$(echo "$SUMMARY_LINE" | awk '{print $(NF-4)+0}')
          PASSED=$(echo "$SUMMARY_LINE" | awk '{print $(NF-3)+0}')
          FAILED=$(echo "$SUMMARY_LINE" | awk '{print $(NF-2)+0}')
          SKIPPED=$(echo "$SUMMARY_LINE" | awk '{print $(NF-1)+0}')
          TOTAL_TIME=$(echo "$SUMMARY_LINE" | awk '{print $2}')

          # Calculate success rate
          if [[ $TOTAL -gt 0 ]]; then
            SUCCESS_RATE=$(awk "BEGIN {printf \"%.2f\", ($PASSED/$TOTAL)*100}")
          else
            SUCCESS_RATE=0
          fi

          echo "total=$TOTAL" >> $GITHUB_OUTPUT
          echo "passed=$PASSED" >> $GITHUB_OUTPUT
          echo "failed=$FAILED" >> $GITHUB_OUTPUT
          echo "skipped=$SKIPPED" >> $GITHUB_OUTPUT
          echo "success_rate=$SUCCESS_RATE" >> $GITHUB_OUTPUT
          echo "total_time=$TOTAL_TIME" >> $GITHUB_OUTPUT

      - name: Generate Allure Report
        run: |
          npx allure generate allure-results --clean -o allure-report

      - name: Deploy Allure Report to Surge
        run: |
          npm install --global surge
          surge ./allure-report https://kwant-automation-dashboard.surge.sh --token $SURGE_TOKEN

      - name: Send Slack Notification
        if: always()
        run: |
          TOTAL="${{ steps.cypress.outputs.total }}"
          PASSED="${{ steps.cypress.outputs.passed }}"
          FAILED="${{ steps.cypress.outputs.failed }}"
          SKIPPED="${{ steps.cypress.outputs.skipped }}"
          SUCCESS_RATE="${{ steps.cypress.outputs.success_rate }}"
          TOTAL_TIME="${{ steps.cypress.outputs.total_time }}"
          TESTS_RAN="${{ steps.cypress.outputs.tests_ran }}"
          TIME="$(date -u '+%Y-%m-%d %H:%M:%S UTC')"

          if [[ "$TESTS_RAN" == "false" ]]; then
            STATUS="⚠️ UAT test automation didn't run properly"
            COLOR="warning"
          elif [[ "$FAILED" -eq 0 && "$TOTAL" -gt 0 ]]; then
            STATUS="✅ UAT test automation completed successfully"
            COLOR="good"
          else
            STATUS="🚨 UAT test automation- Health Check tests failed"
            COLOR="danger"
          fi

          PAYLOAD="{
            \"attachments\": [
              {
                \"color\": \"$COLOR\",
                \"title\": \"$STATUS\",
                \"fields\": [
                  {\"title\": \"Total Tests\", \"value\": \"$TOTAL\", \"short\": true},
                  {\"title\": \"✅ Passed\", \"value\": \"$PASSED\", \"short\": true},
                  {\"title\": \"❌ Failed\", \"value\": \"$FAILED\", \"short\": true},
                  {\"title\": \"⏭️ Skipped\", \"value\": \"$SKIPPED\", \"short\": true},
                  {\"title\": \"📈 Success Rate\", \"value\": \"$SUCCESS_RATE%\", \"short\": true},
                  {\"title\": \"⏳ Total Time\", \"value\": \"$TOTAL_TIME\", \"short\": true},
                  {\"title\": \"🕐 Last Checked\", \"value\": \"$TIME\", \"short\": false}
                ],
                \"actions\": [
                  {
                    \"type\": \"button\",
                    \"text\": \"View Detailed Report\",
                    \"url\": \"https://kwant-automation-dashboard.surge.sh/\"
                  }
                ]
              }
            ]
          }"

          curl -X POST -H 'Content-type: application/json' --data "$PAYLOAD" "$SLACK_WEBHOOK_URL"
