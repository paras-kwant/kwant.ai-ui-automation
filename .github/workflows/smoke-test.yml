name: Cypress Smoke Tests Parallel with Allure

on:
  # push:
  #   branches: [main, master]
  # pull_request:
  #   branches: [main, master]
  workflow_dispatch:

jobs:
  companies-smoke-tests:
    name: 🏢 Companies Smoke Tests
    runs-on: ubuntu-latest
    timeout-minutes: 40
    env:
      EMAIL: ${{ secrets.EMAIL }}
      PASSWORD: ${{ secrets.PASSWORD }}
      GMAIL_USER: ${{ secrets.GMAIL_USER }}
      GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      SURGE_TOKEN: ${{ secrets.SURGE_TOKEN }}
      TWILIO_NUMBER: ${{ secrets.TWILIO_NUMBER }}
      TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
      TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}
      EXPECTED_FROM: ${{ secrets.EXPECTED_FROM }}
      TERM: xterm
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci

      - name: Run Companies Smoke Tests
        run: |
          mkdir -p allure-results
          npx cypress run \
            --spec "cypress/e2e/companies/**/*.cy.js" \
            --browser chrome --headless \
            --expose grepTags=@smoke --env grepTags=@smoke \
            || echo "⚠️ Companies smoke tests finished with failures"

      - name: Upload Companies Allure Results
        uses: actions/upload-artifact@v4
        with:
          name: companies-smoke-allure-results
          path: allure-results

      - name: Upload Companies Screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: companies-smoke-screenshots
          path: cypress/screenshots
          if-no-files-found: ignore


  workers-smoke-tests:
    name: 👷 Workers Smoke Tests
    runs-on: ubuntu-latest
    timeout-minutes: 40
    env:
      EMAIL: ${{ secrets.EMAIL }}
      PASSWORD: ${{ secrets.PASSWORD }}
      GMAIL_USER: ${{ secrets.GMAIL_USER }}
      GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      SURGE_TOKEN: ${{ secrets.SURGE_TOKEN }}
      TWILIO_NUMBER: ${{ secrets.TWILIO_NUMBER }}
      TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
      TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}
      EXPECTED_FROM: ${{ secrets.EXPECTED_FROM }}
      TERM: xterm
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci

      - name: Run Workers Smoke Tests
        run: |
          mkdir -p allure-results
          npx cypress run \
            --spec "cypress/e2e/workers/**/*.cy.js" \
            --browser chrome --headless \
            --expose grepTags=@smoke --env grepTags=@smoke \
            || echo "⚠️ Workers smoke tests finished with failures"

      - name: Upload Workers Allure Results
        uses: actions/upload-artifact@v4
        with:
          name: workers-smoke-allure-results
          path: allure-results

      - name: Upload Workers Screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: workers-smoke-screenshots
          path: cypress/screenshots
          if-no-files-found: ignore


  insights-smoke-tests:
    name: 📊 Insights Smoke Tests
    runs-on: ubuntu-latest
    timeout-minutes: 40
    env:
      EMAIL: ${{ secrets.EMAIL }}
      PASSWORD: ${{ secrets.PASSWORD }}
      GMAIL_USER: ${{ secrets.GMAIL_USER }}
      GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      SURGE_TOKEN: ${{ secrets.SURGE_TOKEN }}
      TWILIO_NUMBER: ${{ secrets.TWILIO_NUMBER }}
      TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
      TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}
      EXPECTED_FROM: ${{ secrets.EXPECTED_FROM }}
      TERM: xterm
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci

      - name: Run Insights Smoke Tests
        run: |
          mkdir -p allure-results
          npx cypress run \
            --spec "cypress/e2e/insights/**/*.cy.js" \
            --browser chrome --headless \
            --expose grepTags=@smoke --env grepTags=@smoke \
            || echo "⚠️ Insights smoke tests finished with failures"

      - name: Upload Insights Allure Results
        uses: actions/upload-artifact@v4
        with:
          name: insights-smoke-allure-results
          path: allure-results

      - name: Upload Insights Screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: insights-smoke-screenshots
          path: cypress/screenshots
          if-no-files-found: ignore


  auth-smoke-tests:
    name: 🔐 Auth Smoke Tests
    runs-on: ubuntu-latest
    timeout-minutes: 40
    env:
      EMAIL: ${{ secrets.EMAIL }}
      PASSWORD: ${{ secrets.PASSWORD }}
      GMAIL_USER: ${{ secrets.GMAIL_USER }}
      GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      SURGE_TOKEN: ${{ secrets.SURGE_TOKEN }}
      TWILIO_NUMBER: ${{ secrets.TWILIO_NUMBER }}
      TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
      TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}
      EXPECTED_FROM: ${{ secrets.EXPECTED_FROM }}
      TERM: xterm
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci

      - name: Run Auth Smoke Tests
        run: |
          mkdir -p allure-results
          npx cypress run \
            --spec "cypress/e2e/auth/**/*.cy.js" \
            --browser chrome --headless \
            --expose grepTags=@smoke --env grepTags=@smoke \
            || echo "⚠️ Auth smoke tests finished with failures"

      - name: Upload Auth Allure Results
        uses: actions/upload-artifact@v4
        with:
          name: auth-smoke-allure-results
          path: allure-results

      - name: Upload Auth Screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: auth-smoke-screenshots
          path: cypress/screenshots
          if-no-files-found: ignore


  merge-reports:
    name: 📊 Merge Allure Reports & Deploy
    needs: [companies-smoke-tests, workers-smoke-tests, insights-smoke-tests, auth-smoke-tests]
    runs-on: ubuntu-latest
    if: always()
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Allure CLI
        run: npm install -g allure-commandline

      - name: Download Companies Allure Results
        uses: actions/download-artifact@v4
        with:
          name: companies-smoke-allure-results
          path: merged-results/companies
        continue-on-error: true

      - name: Download Workers Allure Results
        uses: actions/download-artifact@v4
        with:
          name: workers-smoke-allure-results
          path: merged-results/workers
        continue-on-error: true

      - name: Download Insights Allure Results
        uses: actions/download-artifact@v4
        with:
          name: insights-smoke-allure-results
          path: merged-results/insights
        continue-on-error: true

      - name: Download Auth Allure Results
        uses: actions/download-artifact@v4
        with:
          name: auth-smoke-allure-results
          path: merged-results/auth
        continue-on-error: true

      - name: Download Companies Screenshots
        uses: actions/download-artifact@v4
        with:
          name: companies-smoke-screenshots
          path: merged-results/screenshots/companies
        continue-on-error: true

      - name: Download Workers Screenshots
        uses: actions/download-artifact@v4
        with:
          name: workers-smoke-screenshots
          path: merged-results/screenshots/workers
        continue-on-error: true

      - name: Download Insights Screenshots
        uses: actions/download-artifact@v4
        with:
          name: insights-smoke-screenshots
          path: merged-results/screenshots/insights
        continue-on-error: true

      - name: Download Auth Screenshots
        uses: actions/download-artifact@v4
        with:
          name: auth-smoke-screenshots
          path: merged-results/screenshots/auth
        continue-on-error: true

      - name: Merge Allure Results
        run: |
          mkdir -p merged-results/allure-results
          find merged-results/companies -type f \( -name "*.json" -o -name "*.png" -o -name "*.jpeg" \) -exec cp -n {} merged-results/allure-results/ \; 2>/dev/null || true
          find merged-results/workers -type f \( -name "*.json" -o -name "*.png" -o -name "*.jpeg" \) -exec cp -n {} merged-results/allure-results/ \; 2>/dev/null || true
          find merged-results/insights -type f \( -name "*.json" -o -name "*.png" -o -name "*.jpeg" \) -exec cp -n {} merged-results/allure-results/ \; 2>/dev/null || true
          find merged-results/auth -type f \( -name "*.json" -o -name "*.png" -o -name "*.jpeg" \) -exec cp -n {} merged-results/allure-results/ \; 2>/dev/null || true
          find merged-results/screenshots -type f \( -name "*.png" -o -name "*.jpeg" \) -exec cp -n {} merged-results/allure-results/ \; 2>/dev/null || true

      - name: Set Smoke Environment Info              # ✅ ONLY NEW STEP ADDED
        run: |
          cat > merged-results/allure-results/environment.properties << EOF
          Report.Type=🔥 Smoke Tests
          Environment=UAT
          Branch=${{ github.ref_name }}
          Run.Number=${{ github.run_number }}
          Triggered.By=${{ github.actor }}
          EOF
          cat > merged-results/allure-results/executor.json << EOF
          {
            "name": "GitHub Actions",
            "type": "github",
            "buildName": "🔥 Smoke Run #${{ github.run_number }}",
            "reportName": "🔥 Smoke Test Report"
          }
          EOF

      - name: Generate Allure Report
        run: |
          rm -rf allure-report
          mkdir -p allure-report
          allure generate merged-results/allure-results --clean -o allure-report
          echo "✅ Allure smoke report generated with $(find allure-report -type f | wc -l) files"

      - name: Deploy Smoke Report to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GH_PAT }}
          publish_dir: ./allure-report
          destination_dir: smoke
          force_orphan: false
          keep_files: true
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'

      - name: Extract Allure Summary
        id: summary
        run: |
          SUMMARY="allure-report/widgets/summary.json"
          TOTAL=$(jq '.statistic.total' $SUMMARY)
          PASSED=$(jq '.statistic.passed' $SUMMARY)
          FAILED=$(jq '.statistic.failed' $SUMMARY)
          BROKEN=$(jq '.statistic.broken' $SUMMARY)
          SKIPPED=$(jq '.statistic.skipped' $SUMMARY)
          DURATION_MS=$(jq '.time.duration' $SUMMARY)

          if [ "$TOTAL" -gt 0 ]; then
            SUCCESS_RATE=$(awk "BEGIN {printf \"%.2f\", ($PASSED/$TOTAL)*100}")
          else
            SUCCESS_RATE="0.00"
          fi

          MINUTES=$((DURATION_MS / 60000))
          SECONDS=$(((DURATION_MS % 60000) / 1000))
          TOTAL_TIME="${MINUTES}m ${SECONDS}s"
          TIMESTAMP=$(date -u '+%Y-%m-%d %H:%M:%S UTC')

          echo "total=$TOTAL" >> $GITHUB_OUTPUT
          echo "passed=$PASSED" >> $GITHUB_OUTPUT
          echo "failed=$FAILED" >> $GITHUB_OUTPUT
          echo "broken=$BROKEN" >> $GITHUB_OUTPUT
          echo "skipped=$SKIPPED" >> $GITHUB_OUTPUT
          echo "success_rate=$SUCCESS_RATE" >> $GITHUB_OUTPUT
          echo "total_time=$TOTAL_TIME" >> $GITHUB_OUTPUT
          echo "timestamp=$TIMESTAMP" >> $GITHUB_OUTPUT

      - name: 💬 Send Slack Notification
        if: always()
        run: |
          PREVIEW_URL="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/smoke/"
          TOTAL="${{ steps.summary.outputs.total }}"
          PASSED="${{ steps.summary.outputs.passed }}"
          FAILED="${{ steps.summary.outputs.failed }}"
          BROKEN="${{ steps.summary.outputs.broken }}"
          SKIPPED="${{ steps.summary.outputs.skipped }}"
          SUCCESS_RATE="${{ steps.summary.outputs.success_rate }}"
          TOTAL_TIME="${{ steps.summary.outputs.total_time }}"
          TIMESTAMP="${{ steps.summary.outputs.timestamp }}"

          TOTAL_FAILURES=$((FAILED + BROKEN))
          if [[ "$TOTAL" -eq 0 ]]; then
            STATUS="⚠️ Smoke tests did not run properly"
            COLOR="warning"
          elif [[ "$TOTAL_FAILURES" -eq 0 ]]; then
            STATUS="✅ Smoke tests completed successfully"
            COLOR="good"
          else
            STATUS="🚨 $TOTAL_FAILURES smoke tests failed out of $TOTAL tests"
            COLOR="danger"
          fi

          PAYLOAD=$(jq -n \
            --arg color "$COLOR" \
            --arg status "$STATUS" \
            --arg total "$TOTAL" \
            --arg passed "$PASSED" \
            --arg failed "$FAILED" \
            --arg broken "$BROKEN" \
            --arg skipped "$SKIPPED" \
            --arg success_rate "$SUCCESS_RATE%" \
            --arg total_time "$TOTAL_TIME" \
            --arg timestamp "$TIMESTAMP" \
            --arg preview_url "$PREVIEW_URL" \
            '{
              attachments: [
                {
                  color: $color,
                  title: $status,
                  fields: [
                    {title: "Total Tests", value: $total, short: true},
                    {title: "✅ Passed", value: $passed, short: true},
                    {title: "❌ Failed", value: $failed, short: true},
                    {title: "🟠 Broken", value: $broken, short: true},
                    {title: "⏭️ Skipped", value: $skipped, short: true},
                    {title: "📈 Success Rate", value: $success_rate, short: true},
                    {title: "⏱️ Duration", value: $total_time, short: true},
                    {title: "🕐 Last Checked", value: $timestamp, short: false}
                  ],
                  actions: [
                    {type: "button", text: "View Smoke Report", url: $preview_url}
                  ]
                }
              ]
            }')

          curl -X POST -H 'Content-type: application/json' --data "$PAYLOAD" "$SLACK_WEBHOOK_URL"